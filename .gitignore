# Local Vercel link config
.vercel

# OS files
.DS_Store

# Business/legal docs — never commit these
docs/
Documentation/
about.md
The_Host_Standard_SEO_Strategy_2026.docx
The_Host_Standard_Shareholders_Agreement.docx

# Unused source assets not referenced by the site
assets/Squeeky\ Clean\ viz\ card.png
assets/the\ host\ standard\ logo.png
assets/cindy-headshot.jpg
assets/christina-headshot.png

# Raw design source folders (deliverables, not site assets — use the optimized
# copies in assets/ instead)
THS_Transparent Crest variants/
THS_Headshots/
THS Reviews carousel/

# Intermediate PNG renders from the review SVGs — only the compressed .jpg
# copies in assets/reviews/ are meant to ship
assets/reviews/*.png

# Stray broken-git cleanup artifacts from an earlier setup session
.git-broken-backup/
.git-broken-backup-2/
